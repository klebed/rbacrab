[package]
name = "rbacrab"
version = "0.0.2"
edition = "2024"
description = "Rust 🦀RBAC🦀 library with some crabby🦀🧙 macro magic! Not so blazingly fast yet, but has all 🚀🚀🚀 chances."
authors = [
    "Kirill Lebedev (https://github.com/klebed)", 
    ]
repository = "https://github.com/klebed/rbacrab"
license = "MIT"
readme = "README.md"
keywords = ["library", "rbac", "authorization"]
categories = ["security", "web-programming"]
publish = true

[dependencies]
serde = {version = "1.0", features = ["serde_derive"]}
arc-swap = "1.8"